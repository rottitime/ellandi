version: '3'

services:
  web:
    build:
      context: .
      dockerfile: ./ellandi.Dockerfile
    image: ellandi_web
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000

